import PageStub from "@/components/PageStub";

export default function StartProjectPage() {
  return (
    <PageStub
      eyebrow="Start a Project"
      title="Tell us about the space."
      copy="A qualified project-inquiry form (project type, location, area, scope, budget, timeline) is in development. In the meantime, reach us directly."
      showContact
    />
  );
}
