import InquiryForm from "@/components/InquiryForm";

export const metadata = {
  title: "Start a Project — h-aesthetic studio",
  description: "Tell us about your space. We'll come back with a planning approach before anything gets drawn.",
};

export default function StartProjectPage() {
  return <InquiryForm />;
}
